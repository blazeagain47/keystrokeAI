import React from "react";

import PartyEntry from "@/components/party/PartyEntry";

export const metadata = {
  title: "1v1 Party — blazeKey",
  description: "Create or join a 1v1 typing race on blazeKey.",
};

export default function PartyEntryPage() {
  return <PartyEntry />;
}
